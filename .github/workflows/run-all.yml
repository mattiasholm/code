name: Run All

on:
  workflow_dispatch:

jobs:
  run:
    strategy:
      matrix:
        workflow:
          - azure-arm
          - azure-bicep
          - azure-pulumi
          - azure-terraform
    uses: ./.github/workflows/${{ matrix.workflow }}.yml
    secrets: inherit
